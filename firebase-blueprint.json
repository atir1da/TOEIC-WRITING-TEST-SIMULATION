{
  "entities": {
    "HistoryEntry": {
      "title": "Test History Entry",
      "description": "Represents a completed TOEIC writing task or full test.",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "The unique ID of the user who took the test." },
        "date": { "type": "string", "format": "date-time", "description": "Timestamp of completion." },
        "type": { "type": "string", "enum": ["Module", "Full Test"] },
        "part": { "type": "string", "description": "The task part or 'ALL' for full tests." },
        "results": { 
          "type": "array", 
          "description": "List of evaluation results for each question."
        }
      },
      "required": ["userId", "date", "type", "part", "results"]
    }
  },
  "firestore": {
    "history": {
      "schema": "HistoryEntry",
      "description": "Storage for user test results."
    }
  }
}
